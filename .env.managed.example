# Local managed-state integration harness only. Never use this file as a
# production secret source; provide PDM_POSTGRES_PASSWORD through a secret
# manager or protected deployment environment.
PDM_POSTGRES_DB=pdm
PDM_POSTGRES_USER=pdm
PDM_POSTGRES_PASSWORD=local-managed-harness-placeholder
PDM_HOST_PORT=8001
